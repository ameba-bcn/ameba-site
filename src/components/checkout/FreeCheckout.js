import React from "react";

export default function FreeCheckout() {
  return (
    <div>
      Aixo es un component que s'encarregara de indicar a l'usuari si el
      checkout gratuit s'ha realitzat correctament
    </div>
  );
}
